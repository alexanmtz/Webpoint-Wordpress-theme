/*
 * Main javascript file of Webpoint theme
 */

 $(function(){
   $('.menu').supersubs({
            minWidth:    12,   // minimum width of sub-menus in em units
            maxWidth:    27,
            extraWidth:  1
        }).superfish();
 });
